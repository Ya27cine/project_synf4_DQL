{% extends "base.html.twig" %}

{% block title %}{{ title }}!{% endblock %}


{% block body %}
{{  include("menu/menu.html.twig") }}


            <h1 class="">Page Show Post Id : {{ post.id }}</h1>

            <div class="row ">
                <div class="col-md-6">  
                    <h4> <a href=""> {{ post.title}}</a></h4> 
                    <p> {{ post.content}}</p>
                    <span class="badge bg-warning text-dark text-left">{{ post.published | date("m/d/Y")}}</span>
                </div>
            </div>


    

{% endblock %}
